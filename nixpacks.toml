[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = [
    "mvn clean package -DskipTests -Dmaven.javadoc.skip=true -B"
]

[start]
cmd = "java -Xmx512m -Xms256m -jar target/sweepstakes-0.0.1-SNAPSHOT.jar"