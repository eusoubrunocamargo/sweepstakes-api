[build]
builder = "nixpacks"
buildCommand = "mvn clean install -DskipTests -B -U --no-transfer-progress"

[deploy]
startCommand = "java -Xmx512m -jar target/sweepstakes-0.0.1-SNAPSHOT.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10